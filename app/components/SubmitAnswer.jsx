"use client";
import React from "react";

const SubmitAnswer = () => {
  return (
    <div>
      <button onClick={() => console.log("Clicked on Submit Answer Button")}>
        Submit Answer
      </button>
    </div>
  );
};

export default SubmitAnswer;
